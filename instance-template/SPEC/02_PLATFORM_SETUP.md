# {{INSTANCE_NAME}} — Spec Item #2: Platform Setup

> **Status: v0.1** — to confirm with the user. Platform: DeepSeek Harness (DSH).

## Decisions (to confirm)

| # | Decision | Value |
|---|---|---|
| P1 | Workspace | **{{ROOT}}** — select in the GUI (Choose workspace) |
| P2 | Preset | id **{{PRESET_ID}}** — identity/behavior + tool kit + skills |
| P3 | Profile | **`web`** (Web GUI) |
| P4 | Model + API key | **user action** — Settings → Models → DeepSeek API key |

## Guardrails (from item #1, unchanged)

Own folder only · propose→verify→dispose · evidence-backed claims · full audit.

## History

- Scaffolded at instance bootstrap; **awaiting user confirmation** (P1–P4).
