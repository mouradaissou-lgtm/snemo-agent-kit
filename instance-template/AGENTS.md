# {{INSTANCE_NAME}} — workspace pointer (standalone SNemo instance)

> This is a **standalone instance**: its OWN brain, its OWN memory.
> **Do NOT connect to any shared brain** (~/.dsh/core-path). Identity comes from the mode ({{PRESET_ID}}).
> **FIRST ACTION: read the full ledger `state/memory/beliefs.md` — the digest below is a boot
> aid, NOT the memory.** Then follow `AGENT_CORE.md` (this root) read-first list.

Read order (ORIENT):
1. `AGENT_CORE.md`
1b. `SPEC/01..05` (identity, persona, writing style — read before a reply to a person)
2. `state/memory/beliefs.md` (the only memory of this instance — ENTIRE)
3. `READ_FIRST.md` (docs to read before any work)
4. `{{FRAMEWORK_FILE}}` (the use-case framework — method, no numbers)
5. `{{DATA_DOCTRINE}}` (how to read data)
6. `state/projects.md`
7. `state/logs/ROUND_LOG.md`
